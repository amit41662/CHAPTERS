#!/usr/bin/env node

const fs = require("fs");
const path = require("path");

const root = process.cwd();
console.log("CHAPTERS app reset script");
console.log("This would reset the project to a blank state.");
console.log("Root:", root);
